# CODEOWNERS
#
# These paths are the trust-critical zones described in
# docs/principles.md: the closed skill vocabulary, the signature database
# that feeds it, the schema that bounds every bundle field, the secret
# scan and public-remote guardrails, the only code that ever touches the
# network (login/submit/http-client), and the privacy test suite that
# proves all of the above. A PR touching any of these requires review
# from the maintainer listed below before merge, regardless of who else
# has approved — per CLAUDE.md's sensitive-zones rule.

/schema/                @jpbelmo
/taxonomy.json          @jpbelmo
/signatures/            @jpbelmo
/src/secret-scan.ts     @jpbelmo
/src/public-remote.ts   @jpbelmo
/src/submit.ts          @jpbelmo
/src/submit-command.ts  @jpbelmo
/src/login.ts           @jpbelmo
/src/http-client.ts     @jpbelmo
/test/privacy/          @jpbelmo
/.github/               @jpbelmo
