node_modules/
dist/
build/
.DS_Store
# .claude/ is local session configuration and stays private, EXCEPT
# .claude/skills/ — contributor-facing skills, published on purpose
# (see CONTRIBUTING.md, "Working with an AI agent").
.claude/*
!.claude/skills/
